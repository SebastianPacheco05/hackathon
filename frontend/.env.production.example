# =============================================================================
# REVITAL ECOMMERCE FRONTEND - PRODUCCIÓN
# =============================================================================
# Copia como .env.production. Next.js lo carga en build/start (NODE_ENV=production).

# API del backend (URL pública de producción)
NEXT_PUBLIC_API_URL=https://api.tu-dominio.revital.cloud/api

# Dejar en false en producción (expone chat IA sin autenticación)
NEXT_PUBLIC_ADMIN_AI_PUBLIC_CHAT=false

# Wompi producción (no usar pub_test_)
NEXT_PUBLIC_WOMPI_PUBLIC_KEY=pub_prod_xxxx
